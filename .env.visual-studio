# Configuração local sem segredos para o perfil conjunto do Visual Studio.
# O navegador usa a mesma origem do frontend; o Vite encaminha /api à API local.
NEXT_PUBLIC_API_URL=http://localhost:3000
HOUSESTUFF_API_PROXY_TARGET=http://localhost:5049
